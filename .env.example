# PostgreSQL connection used by the API and Alembic. Port 5433 is the host mapping used by docker-compose.yml.
DATABASE_URL=postgresql+asyncpg://postgres:postgres@localhost:5433/appointment_scheduler
